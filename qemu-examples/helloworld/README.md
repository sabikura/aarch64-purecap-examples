# `helloworld-morello-qemu`

A minimal example that prints a short message over UART on the CHERI Morello Qemu using the aarch64-purecap-rt runtime. The program boots in EL1 hybrid mode and switches into pure-capability EL1 to print the message.
